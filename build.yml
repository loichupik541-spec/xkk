name: Build Windows EXE

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas openpyxl pyinstaller
    
    - name: Build EXE
      run: |
        pyinstaller --onefile --name 邮箱筛查工具 email_screening.py
    
    - name: Prepare release files
      run: |
        mkdir 邮箱筛查工具_Windows版
        copy dist\邮箱筛查工具.exe 邮箱筛查工具_Windows版\
        mkdir 邮箱筛查工具_Windows版\input
        mkdir 邮箱筛查工具_Windows版\output
        copy 使用说明.txt 邮箱筛查工具_Windows版\
        copy 运行.bat 邮箱筛查工具_Windows版\
    
    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: 邮箱筛查工具_Windows版
        path: 邮箱筛查工具_Windows版/
    
    - name: Create ZIP
      run: |
        Compress-Archive -Path 邮箱筛查工具_Windows版 -DestinationPath 邮箱筛查工具_Windows版.zip
    
    - name: Upload ZIP
      uses: actions/upload-artifact@v3
      with:
        name: 邮箱筛查工具_Windows版_ZIP
        path: 邮箱筛查工具_Windows版.zip
